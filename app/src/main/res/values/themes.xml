<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Bykea" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/brandPrimary</item>
        <item name="colorOnPrimary">@color/brandOnPrimary</item>
        <item name="android:statusBarColor">@color/brandPrimary</item>
        <item name="android:navigationBarColor">@color/brandSurface</item>

        <!-- Accessibility-friendly defaults -->
        <item name="android:textAllCaps">false</item>
        <item name="android:fontFamily">sans_serif_medium</item>
    </style>
</resources>
